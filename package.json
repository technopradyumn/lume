{
  "name": "lume-monorepo",
  "version": "1.0.0",
  "description": "Lume Video Streaming & Social Platform - Full Stack MERN & Flutter Mobile App",
  "type": "module",
  "scripts": {
    "setup": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "npx concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "npm --prefix backend run dev",
    "frontend": "npm --prefix frontend run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
